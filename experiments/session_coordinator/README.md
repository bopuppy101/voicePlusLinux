# Typed-request lifecycle prototype

Work in progress: extends the disposable sandbox with request identity, revision,
correlated interpretation jobs, cancellation, policy invalidation, and optional
proposal-bound confirmation. It will remain confined to generated temporary
files. Python is an experimental choice, not the production language decision.

The default example interpreter is deterministic test data, not AI. Real model
integration and production transport/authentication remain separate work.
